import 'package:flutter/material.dart';

class HotelColors {
  // Brand core
  static const primary = Color(0xFF3092E3);
  static const secondary = Color(0xFF305AE3);
  static const accent = Color(0xFF4F00E5);

  // Semantic
  static const success = Color(0xFF138F4F);
  static const warning = Color(0xFFFF7E00);

  // Backgrounds
  static const background = Color(0xFFF6F8FD);
  static const backgroundSecondary = Color(0xFFEEF4FF);

  // Surfaces
  static const cardBackground = Color(0xFFFFFFFF);
  static const surfaceBlue = Color(0xFFF4F9FF);
  static const surfacePurple = Color(0xFFF7F3FF);
  static const surfaceGreen = Color(0xFFF2FBF6);
  static const surfaceOrange = Color(0xFFFFF7EF);

  // Text
  static const textPrimary = Color(0xFF18212F);
  static const textSecondary = Color(0xFF60708A);
  static const textOnDark = Color(0xFFFFFFFF);

  // Borders
  static const borderSoft = Color(0xFFDCE6F5);
  static const borderStrong = Color(0xFFC9D8F0);

  // Soft overlays / glows
  static const glowBlue = Color(0x1A3092E3);
  static const glowPurple = Color(0x144F00E5);
  static const glowGreen = Color(0x14138F4F);
  static const glowOrange = Color(0x14FF7E00);
}